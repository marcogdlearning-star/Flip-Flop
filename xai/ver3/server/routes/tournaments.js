import express from 'express';

const router = express.Router();

// Placeholder for tournament routes
router.get('/', (req, res) => {
  res.json({ message: 'Tournaments API' });
});

export default router;
